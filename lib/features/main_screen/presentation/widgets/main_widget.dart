import 'package:flutter/material.dart';

class MainWedgit extends StatelessWidget {
  const MainWedgit({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: ElevatedButton(
        onPressed: () {},
        child: const Text("Hello"),
      ),
    );
  }
}
